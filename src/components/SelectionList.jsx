import React from "react";

export default function SelectionList(list, selected) {
  const newList = list.concat({ selected });

  //     <ul>
  //       {list.map((item) => (
  //         <li key={item.id}>{item.name}</li>
  //       ))}
  //     </ul>
  //   );

  return <div></div>;
}
